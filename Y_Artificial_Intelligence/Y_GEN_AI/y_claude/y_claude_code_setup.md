# Setup in UBUNTU 
    ## Using NPM (Node package manager) :

        - SETUP NPM in UBUNTU :
            > sudo apt update 
            > sudo apt install nodejs npm -y
            > node -v
            > npm -v