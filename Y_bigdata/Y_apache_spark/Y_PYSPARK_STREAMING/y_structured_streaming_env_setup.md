1) Create VENV to has libraries installed :
        > python3 -m venv y_env_for_struct_stream

2) Activate the venv for installing required libs :
        > source ./y_env_for_struct_stream/bin/activate

3) Install Pyspark library :
        > pip install pyspark